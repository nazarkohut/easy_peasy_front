import { FormTextLinkDirective } from './form-text-link.directive';

describe('FormTextLinkDirective', () => {
  it('should create an instance', () => {
    // const directive = new FormTextLinkDirective();
    // expect(directive).toBeTruthy();
  });
});
