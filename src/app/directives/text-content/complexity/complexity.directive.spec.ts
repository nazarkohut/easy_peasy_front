import { ComplexityDirective } from './complexity.directive';

describe('ComplexityDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ComplexityDirective();
  //   expect(directive).toBeTruthy();
  // });
});
